# Reproducibility

Run `python src/run_experiment.py` from the repository root after installing `requirements.txt`. Random seeds are fixed where supported. Data provenance, target construction, and split logic are explicit in code. Never copy a numerical result into academic material unless it was generated by this repository on the stated data.
